#include "CustomVector.h"
void main()
{
	CustomVector(vector<int>(1));
	CustomVector(vector<int>(5),vector<double>(1));
	CustomVector paracust(10);
	CustomVector copyconst = paracust;
	system("pause");

}
